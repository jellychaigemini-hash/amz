# Source Generated with Decompyle++
# File: _recover.pyc (Python 3.13)

data = ()()
nonce = None(str(filepath.relative_to(WIKI_DIR)))
encrypted = None(data, nonce)
enc_path = None(filepath.suffix + '.enc')
None(encrypted)
return enc_path
except ImportError:
    encrypted = _pure_aes_ctr(data, nonce)
