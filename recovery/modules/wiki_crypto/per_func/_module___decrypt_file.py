# Source Generated with Decompyle++
# File: _recover.pyc (Python 3.13)

data = ()()
original_rel_path = enc_path.name.replace('.enc', '')
rel = None(Path(original_rel_path))
nonce = None(rel)
return None(data, nonce)
except Exception:
    rel = enc_path.stem
return None
