# Source Generated with Decompyle++
# File: _per_func.pyc (Python 3.13)

.job_lock()
jobs.get(job_id)
if not job:
    pass
(jsonify({
    'error': 'Job not found' }), 404)('asin', 'unknown') / f'''_prompts.json''' = (), None, ().job_lock, 
if None():
    pass
return json.loads(None(prompt_path.read_text('utf-8')))
return jsonify({
    'message': 'Prompts not yet generated',
    'prompts': [] })
with jsonify:
    continue
