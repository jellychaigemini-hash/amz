# Source Generated with Decompyle++
# File: _per_func.pyc (Python 3.13)

.job_lock()
job_queues.get(job_id)(None, None, None)
return stream_with_context(generate(None()), 'text/event-stream', {
    'X-Accel-Buffering': 'no',
    'Cache-Control': 'no-cache' })
with (job_id, q, generate):
    (), None, ().job_lock, 
    continue
