# Source Generated with Decompyle++
# File: _per_func.pyc (Python 3.13)

payload = {
    'message': message,
    (): progress,
    None: None }
payload['data'] = data
.job_lock()
job_queues.get(job_id)
if q:
    pass
q.put(payload)
.job_lock()
if job_id in jobs:
    pass
(jobs <INVALID> job_id).update({
    'message': message,
    'progress': progress,
    'step': step })
job_lock, None, job_lock.job_lock, (None, None, None)
return None
with None, None, None.job_lock, :
    continue
    with None:
        return None
